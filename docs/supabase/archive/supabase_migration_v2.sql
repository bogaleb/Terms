-- ARCHIVE ONLY — NOT FOR PRODUCTION USE
-- This draft uses a different schema naming (orgs/org_members) and can conflict with current schema.
-- Keep for reference; do not run in production project without adapting to canonical names.

\ir ../../supabase_migration_v2.sql
